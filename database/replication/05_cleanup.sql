-- Only after the PlanetScale verification window has completed.
-- Target:
drop subscription if exists phaseo_from_supabase;

-- Run separately on the Supabase source:
-- drop publication if exists phaseo_to_planetscale;
