# Issue thread snapshot

Issue: #1574 Fix errors when opening a request row
Opened by: @DanielButler1

## Original issue body

## Problem
Opening a request row can produce an error instead of showing request details.

## Acceptance criteria
- A valid request row opens without an unhandled error.
- Missing or optional request fields are handled safely.
- Failed detail loading produces a recoverable state.
- Regression coverage includes the request shape that triggered the bug.