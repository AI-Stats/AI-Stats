---
icon: "bot"
title: "Build your first agent"
description: "Create and run a tool-ready agent with the Go Agent SDK."
---

Use this guide to run a Go agent while keeping tools and state inside your service.

```go
client, err := phaseoagent.CreateGatewayAgentClient(
  phaseoagent.GatewayAgentClientOptions{},
)
if err != nil { panic(err) }

agent := phaseoagent.CreateAgent(phaseoagent.AgentDefinition{
  ID: "quickstart-agent",
  Model: "openai/gpt-5.4-nano",
  Instructions: "Answer concisely and helpfully.",
})

result, err := agent.Run(context.Background(), phaseoagent.RunOptions{
  Input: "Give me one fun fact about cURL.",
  Client: client,
})
if err != nil { panic(err) }

fmt.Println(result.Output)
```

## Next

- [Define tools](./agent-sdk-tools)
- [Stream a run](./agent-sdk-streaming)
- [Pause and continue](./agent-sdk-state-and-approval)
- [Control a run](./agent-sdk-run-controls)
- [Observe runs](./agent-sdk-observability)
