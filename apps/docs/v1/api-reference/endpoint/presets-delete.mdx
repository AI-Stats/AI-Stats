---
title: "Archive preset"
sidebarTitle: "Archive preset"
badge: "Beta"
openapi: "DELETE /presets/{id}"
description: "Soft-delete a workspace preset."
---
<Note>This endpoint requires a management API key with `presets:delete`.</Note>

Pass `confirm_name` for optional exact-name confirmation.
