---
title: "List workspace join requests"
sidebarTitle: "List join requests"
badge: "Beta"
openapi: "GET /workspaces/{id}/join-requests"
description: "List and filter workspace join requests."
---
<Note>This endpoint requires a management API key with `workspaces:read`.</Note>

Use the `status` filter to select pending, approved, or denied requests.
