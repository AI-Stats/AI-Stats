---
title: "Reject workspace join request"
sidebarTitle: "Reject join request"
badge: "Beta"
openapi: "POST /workspaces/{id}/join-requests/{request_id}/reject"
description: "Reject a pending workspace join request."
---
<Note>This endpoint requires a management API key with `workspaces:write`.</Note>

Decisions are final and recorded in the workspace audit log.
