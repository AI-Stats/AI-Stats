---
title: "Update dynamic route"
sidebarTitle: "Update route"
badge: "Beta"
openapi: "PATCH /routing/dynamic-routes/{id}"
description: "Update route metadata or create a new configuration version."
---
<Note>This endpoint requires a management API key with `settings:write`.</Note>

Changing `config` creates a new immutable version. Metadata-only changes do not create a version.
