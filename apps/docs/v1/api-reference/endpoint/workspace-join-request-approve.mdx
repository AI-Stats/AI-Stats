---
title: "Approve workspace join request"
sidebarTitle: "Approve join request"
badge: "Beta"
openapi: "POST /workspaces/{id}/join-requests/{request_id}/approve"
description: "Approve a pending join request atomically."
---
<Note>This endpoint requires a management API key with `workspaces:write`.</Note>

Approval validates invite expiry and remaining uses, applies the invite role, and records the decision in one transaction.
