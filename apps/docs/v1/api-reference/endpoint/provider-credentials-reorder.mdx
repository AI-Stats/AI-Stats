---
title: "Reorder provider credentials"
sidebarTitle: "Reorder credentials"
badge: "Beta"
openapi: "POST /byok/reorder"
description: "Replace credential priority within a provider routing group."
---

Supply every credential ID in the selected provider and routing group exactly once. The first ID receives the highest priority. Partial or duplicate orders are rejected.
