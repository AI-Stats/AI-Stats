---
title: "Create dynamic route"
sidebarTitle: "Create route"
badge: "Beta"
openapi: "POST /routing/dynamic-routes"
description: "Create a versioned dynamic routing policy."
---
<Note>This endpoint requires a management API key with `settings:write`.</Note>

The initial configuration is stored as immutable version 1. Deploy it separately when it is ready for attached keys.
