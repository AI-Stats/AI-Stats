---
title: "List workspace invites"
sidebarTitle: "List invites"
badge: "Beta"
openapi: "GET /workspaces/{id}/invites"
description: "List workspace invite metadata."
---
<Note>This endpoint requires a management API key with `workspaces:read`.</Note>

Only the masked token preview is returned. Plaintext invite tokens cannot be retrieved later.
