---
title: "Update preset publisher"
sidebarTitle: "Update publisher"
badge: "Beta"
openapi: "PUT /presets/publisher"
description: "Rename the workspace public-preset publisher handle."
---
<Note>This endpoint requires a management API key with `presets:write` and an associated user identity.</Note>

The previous handle is retained as an alias so existing public preset references continue to resolve.
