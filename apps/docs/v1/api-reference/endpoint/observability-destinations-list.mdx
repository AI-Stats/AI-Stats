---
title: "List observability destinations"
sidebarTitle: "List destinations"
badge: "Beta"
openapi: "GET /observability/destinations"
description: "List executable workspace observability destinations."
---
<Note>This endpoint requires a management API key with `settings:read`.</Note>

Destination credentials are never returned. `configured` indicates whether encrypted configuration exists.
