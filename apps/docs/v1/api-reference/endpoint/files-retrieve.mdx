---
title: "Retrieve a file"
sidebarTitle: "Retrieve a file"
badge: "Beta"
openapi: "GET /files/{file_id}"
description: "Retrieve metadata for a file that belongs to the authenticated workspace."
---

This route refreshes the upstream file metadata and enforces workspace ownership before proxying the response.

