---
title: "Update preset draft"
sidebarTitle: "Update preset"
badge: "Beta"
openapi: "PATCH /presets/{id}"
description: "Update preset draft metadata and configuration."
---
<Note>This endpoint requires a management API key with `presets:write`.</Note>

Use `replace_config` to replace the complete draft config. Otherwise supplied config fields are merged at the top level.
