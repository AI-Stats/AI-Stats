---
title: "Create preset"
sidebarTitle: "Create preset"
badge: "Beta"
openapi: "POST /presets"
description: "Create a reusable workspace gateway preset."
---
<Note>This endpoint requires a management API key with `presets:write` and an associated user identity.</Note>

Preset configuration can contain bounded prompt, parameter, routing, and plugin defaults. Public presets use the workspace publisher handle.
