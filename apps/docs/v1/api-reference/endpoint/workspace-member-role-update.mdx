---
title: "Update workspace member role"
sidebarTitle: "Update member role"
badge: "Beta"
openapi: "PATCH /workspaces/{id}/members/{user_id}"
description: "Change a workspace member between admin and member."
---
<Note>This endpoint requires a workspace owner and a management API key with `workspaces:write`.</Note>

The owner's role is immutable.
