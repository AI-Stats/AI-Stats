---
title: "Add workspace members"
sidebarTitle: "Add members"
badge: "Beta"
openapi: "POST /workspaces/{id}/members/add"
description: "Add existing users to a workspace."
---
<Note>This endpoint requires a management API key with `workspaces:write` and an owner or admin role.</Note>

The role can be `admin` or `member`. Workspace ownership cannot be assigned through this endpoint.
